import React from "react";
import CharacterRankReference from "./CharacterRankReference";

const ReferencePage: React.FC = () => (
  <div>
    <h1 style={{ textAlign: "center", marginTop: "1em" }}>参考情報</h1>
    <CharacterRankReference />
  </div>
);

export default ReferencePage;
